# Copy to .env and fill in. The server loads .env via dotenv.
# Optional: the app also accepts a key at runtime via Settings > Secrets.
GEMINI_API_KEY=

# Port the server listens on (default 3000).
PORT=3000
