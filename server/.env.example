# Copy to server/.env for local work. Compose passes these in already.
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=koda
JWT_SECRET=dev-only-change-me-not-a-real-secret-32b
ENVIRONMENT=development
